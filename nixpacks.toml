[phases.setup]
nixPkgs = ["python312", "gcc", "nodejs_20"]

[phases.install]
cmds = [
    "python -m venv /opt/venv",
    ". /opt/venv/bin/activate && python -m pip install -U pip",
    ". /opt/venv/bin/activate && python -m pip install .",
]

[phases.build]
cmds = []

[start]
cmd = "/opt/venv/bin/python railway_server.py"

